# @kiris/app

The authoring app at `app.kiris.ai`. **Step 2** deliverable. Currently a
placeholder so the Turborepo workspace is complete.

Step 2 ships:

- 3-column editor layout (DESIGN §16.11): outline rail, slide canvas, properties + AI helpers
- Express AI flow (single-screen + progress narration + lands in editor)
- Guided AI flow (outline tree, per-field ✨ helpers, "Polish all")
- Preview player (mimics SCORM player behavior)
- Per-seat cap workflow (soft warn 80%, hard block 100%, request modal, admin queue)
- Module CRUD with mocked persistence; real persistence wires up in Step 3.
